# Використовуйте офіційний `Node.js` образ як базовий
FROM node:14

# Встановіть робочий каталог у контейнері
WORKDIR /usr/src/app

# Скопіюйте package.json та package-lock.json до контейнера
COPY package*.json ./

# Встановіть залежності
RUN npm install

# Скопіюйте решту програми до контейнера
COPY . .

# Відкрийте порт, на якому працює ваш додаток
EXPOSE 3000

# Команда для запуску вашого додатку
CMD ["npm", "start"]